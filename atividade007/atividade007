#include <stdio.h>

/*Elabore um programa utilizando Structs onde o usuário realizará o cadastro de até 20 estudantes, 
cada estrutura de estudante deverá conter nome, matricula, 
idade e disciplina (disciplina deverá ser outra Struct cotendo nome da disciplina, 
carga horária e um vetor notas[4] para lançamento das notas n1, n2, n3 e n4), após os registros apresente o nome, 
média das quatro notas e idade de todos os estudantes, por fim apresente o(s) nome(s) do(s) estudante(s) 
com a maior média identificada (independente da disciplina).*/

#define TAM 20

int main(){

    int i=0;
    
    struct Disciplina{
        char nome[30];
        int carga_horaria;
        float notas[4];
    };
    

    struct Aluno{
        char nome[30];
        int matricula;
        int idade;
        struct Disciplina disciplina;
    };

    struct Estudante estudante[20];

    printf("Cadastro\n");

    for(i=0;i<2;i++){
        printf("Nome: ");
        scanf("%[^\n]s",&estudantes[i].nome)
    }

}